npx create-expo-app@latest
echo 'export ANDROID_HOME=$HOME/Android/Sdk' >> ~/.bashrc
echo 'export PATH=$ANDROID_HOME/platform-tools:$PATH' >> ~/.bashrc
source ~/.bashrc

npx expo
npm run reset-project

## Basic components
```
import { Link } from 'expo-router';
import { Button, Image, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';


export default function HomeScreen() {
  return (
    <ScrollView>
      <View style={style.container}>
        <TextInput placeholder='Type here'></TextInput>
        <Text>Edit text</Text>
        <Link href={'/details'}>Details</Link>
        <Button title='Open drawer' onPress={()=>{console.log("LOG")}}/>
          <Image style={style.image} source={require('@/assets/images/icon.png')}></Image>
      </View>
    </ScrollView>
  );
}

const style = StyleSheet.create({
  container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: 'gray'
  },
  image: {
    width: "100%"
  }
})
```

## Navigation
React native koristi file based navigation, slicno kao next.js
U fajlu _layout.tsx definise se tip Layouta. Moze biti Stack, Tabs, ili Drawer. U layout mozes da naznacis bas kako ce ruta do fajla izgledati ali ne mora, react to automatski radi.
```
import { Tabs } from 'expo-router';
import React from 'react';

import { HapticTab } from '@/components/haptic-tab';
import { IconSymbol } from '@/components/ui/icon-symbol';
import { Colors } from '@/constants/theme';
import { useColorScheme } from '@/hooks/use-color-scheme';

export default function TabLayout() {
  const colorScheme = useColorScheme();

  return (
    <Tabs
      screenOptions={{
        tabBarActiveTintColor: Colors[colorScheme ?? 'light'].tint,
        headerShown: false,
        tabBarButton: HapticTab,
      }}>
      <Tabs.Screen
        name="index"
        options={{
          title: 'Home',
          tabBarIcon: ({ color }) => <IconSymbol size={28} name="house.fill" color={color} />,
        }}
      />
      // ... ne mora da se dodaje, sam ce provaliti
    </Tabs>
  );
}
```

